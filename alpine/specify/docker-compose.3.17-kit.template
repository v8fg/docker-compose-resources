version: '3.8'
services:
  ${v_container}:
    image: ${v_my_image}
    container_name: ${v_container}
    restart: always # no|on-failure[:max-retries]|always|unless-stopped
    environment:
      - TZ=${v_tz}
    stdin_open: true
    tty: true 
    command: [ "/bin/bash"]
