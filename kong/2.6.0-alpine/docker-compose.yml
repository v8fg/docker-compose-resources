version: '3.7'
services:
  kong:
    build: .
    container_name: kong-2.6.0-alpine
    environment:
      TZ: Asia/Shanghai
    image: v8fg/kong:2.6.0-alpine
    stdin_open: true
    tty: true 
    restart: 'no' # no|on-failure[:max-retries]|always|unless-stopped
    command: [ "/bin/bash"]
